"use client"
import React from 'react';

const Button = () => {
    return (
        <div>
            <button onClick={()=>{alert("Cliecked")}}>Click</button>
        </div>
    );
};

export default Button;